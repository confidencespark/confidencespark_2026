// src/constants/storageKeys.js
/**
 * AsyncStorage Keys
 *
 * Central registry of all keys used for local persistence.
 */
export const STORAGE_KEYS = {
  AUTH_TOKEN: '@auth_token',
  USER_EMAIL: '@user_email',
};
